# AirPath Sample Scenarios

These `.airpath.json` files are canonical JSON examples for manual import, validation, demos, and report review.

Each sample includes expected interpretation, recommended report output, and validation relevance in `metadata`.
